import { SeoHeroMotion } from "@/components/platform-motion";

/** Mantido para compatibilidade — delega ao motion alinhado ao MVP. */
export function ConteudoMotionShowcase() {
  return <SeoHeroMotion />;
}
